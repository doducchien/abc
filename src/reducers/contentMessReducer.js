var contentMessState = [];

const contentMessReducer = (state = contentMessState, action) =>{
    switch(action.type){
        case 0:{
            break;
        }
        default:{
            return [...state];
        }
    }
}
export default contentMessReducer;